#ifndef SHARED_H_
#define SHARED_H_

#include <vector>
#include <string>

namespace cc
{
    using string   = std::string;
    using byte     = unsigned char;
    using ByteVect = std::vector<byte>;
}

#endif // !SHARED_H_